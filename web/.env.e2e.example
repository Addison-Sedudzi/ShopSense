# Copy to web/.env.e2e.local and fill in real values. Never commit web/.env.e2e.local.
#
# These e2e specs drive a real browser against the real dev server, the real
# API, and the real Supabase project (never mocked) -- this must be a real
# account that already exists in that Supabase project.
E2E_EMAIL=
E2E_PASSWORD=
